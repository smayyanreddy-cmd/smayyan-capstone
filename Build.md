 Build Log

A running log of progress on the Creative Continuity Agent capstone (originally scoped as a Car Modification Visualiser — see the 2026-09-17 entry for the pivot). See `Plan.md` for the full current plan.

## 2026-09-11

- Initialized the git repository, pushed `feature/capstone-plan` branch to GitHub (`smayyanreddy-cmd/smayyan-capstone`).
- Wrote and approved the project plan (`Plan.md`): a Next.js + TypeScript app where users upload a photo of their car, pick mods (wheels, paint/wrap, ride height, body kit, exhaust, tint, spoiler), and an AI image-editing model generates a realistic "after" photo. Stack: Next.js (App Router), Tailwind, NextAuth, Prisma + Supabase (Postgres + storage), deployed on Vercel.
- Defined milestones: (1) scaffold + auth, (2) core visualize flow for one mod category, (3) expand mod categories, (4) garage/persistence, (5) gallery/sharing, (6) polish + testing.
- Next up: Milestone 1 — scaffold the Next.js app, set up Prisma schema, wire up Supabase, get NextAuth sign-up/sign-in working.

## 2026-09-17

- Pivoted the capstone idea. Replaced the Car Modification Visualiser concept with a new project: the **Creative Continuity Agent** — a tool that ingests scattered creative material (sketches, AI-render experiments, references) with near-zero friction, clusters it by visual + conceptual similarity, tracks how a concept evolves over time, and resurfaces related past work when it's actually useful.
- No code carries over from the old plan; this is effectively a fresh start on a new direction. `Plan.md` has been rewritten in full to describe the new problem statement, MVP scope, user flow, architecture sketch, and open questions.
- Wedge chosen to keep v1 narrow: track the evolution of a single ongoing project that's actively being documented (proof case: personal SR project + automotive fusion sketches), rather than building a general-purpose creative tool.
- MVP scope defined: ingestion (upload + auto-tag), context capture (optional phrase + AI-written description), clustering (image embeddings + LLM conceptual similarity), an evolution timeline view, and contextual resurfacing of 2-3 relevant past pieces triggered by activity.
- Sketched architecture: single web interface for capture + viewing, S3 for image storage, Postgres for metadata, CLIP-style embeddings for visual similarity, Claude for descriptions/tagging/resurfacing explanations.
- Explicitly out of scope for v1: general notebook/moodboard features, multi-user/team collaboration, scheduled notifications.
- Next up: validate the concept before building anything — manually gather existing SR + automotive material into a test set and prototype clustering only (no upload pipeline yet) to see if it surfaces something non-obvious about the existing work.
- Built the clustering validation prototype (`prototype/cluster.py`): given a folder of images, it computes CLIP embeddings (`clip-ViT-B-32` via `sentence-transformers`), groups them with agglomerative clustering on cosine distance, and orders each cluster by file timestamp to approximate an evolution timeline. Optional `--captions captions.json` (filename -> one-line phrase) plus `--describe` calls Claude to write a short combined description per item (needs `ANTHROPIC_API_KEY`). Outputs a readable console summary and `prototype/clusters.json`.
- Verified with `py -3` (Python 3.13, via the `py` launcher — no bare `python`/`pip` on this machine) that the script compiles; full dependency install (`pip install -r prototype/requirements.txt`, notably `sentence-transformers`/torch) not yet run since it's a large download and there's no test image set in the repo yet.
- Next up: gather the SR + automotive test set into a folder, run `py -3 -m pip install -r prototype/requirements.txt` then `py -3 prototype/cluster.py --input <folder>`, and eyeball whether the clusters/timeline surface anything non-obvious before writing any ingestion pipeline.
- Added `.env` support: `.gitignore` (so `.env` never gets committed), an `.env.example` template, a local `.env` with an empty `ANTHROPIC_API_KEY` placeholder to fill in, and `python-dotenv` wired into `prototype/cluster.py` so the key loads automatically when set — only needed for the optional `--describe` step, not plain clustering.
- Ran the first real validation pass: installed `prototype/requirements.txt` and ran the clustering script against a 24-image test set ("Emotion Spiral", WhatsApp exports spanning 2026-08-28 to 2026-09-12).
- Caught and fixed a real bug in the process: `file_timestamp()` was using file mtime, which reflects when the files were downloaded/copied (all showed as today), not when each piece was actually made — the "evolution timeline" was meaningless. Fixed by parsing the real capture date/time out of the WhatsApp export filename pattern (`WhatsApp Image YYYY-MM-DD at H.MM.SS AM/PM`), falling back to mtime only when a filename doesn't match.
- Result on the fixed run: CLIP (default `--threshold 0.35`) split the 24 images into 3 clusters — one large cluster of 18 items spanning almost the whole date range, one singleton (the earliest image, 08-28), and one 5-item cluster mixing dates from 09-04 and 09-12.
- Confirmed with the project owner that all 24 images are actually the same single ongoing project ("Emotion Spiral") — so the 3-way split at default threshold was a false fragmentation, not a real signal. Re-ran at `--threshold 0.5`: all 24 collapsed into one cluster, correctly keeping the project together across the full 08-28 to 09-12 date range. Takeaway: pure CLIP visual similarity alone can wrongly split a single evolving concept at tighter thresholds — it doesn't know about conceptual/character continuity, only pixel-level similarity — so the LLM conceptual layer from the plan matters, not just embeddings.
- Next real test: with the project correctly kept as one cluster, the interesting question is the **evolution view** — does the timestamp-ordered sequence read as a visible progression, and do AI-written descriptions (`--describe`, needs `ANTHROPIC_API_KEY` in `.env`) capture that progression usefully. Not yet run.
- Moved on to building the actual app (`web/`) — a Next.js (App Router, TypeScript, Tailwind) all-in-one app: API routes handle upload + DB, `@huggingface/transformers` runs CLIP (`Xenova/clip-vit-base-patch32`) in-process for embeddings (no separate Python service), `@anthropic-ai/sdk` generates per-item descriptions, images save to `public/uploads/`, metadata to a local SQLite DB.
- Hit a real environment issue: `better-sqlite3`'s native build silently produced no binary on this machine (MSBuild reported success but skipped actual compilation — a toolchain quirk, not chased further). Switched to Node 24's built-in `node:sqlite` module instead, which needs no native compilation and works identically for this app's needs (dropped `better-sqlite3` and `@types/better-sqlite3`, bumped `@types/node` to get its type definitions).
- Implemented: `POST /api/projects` (create) + `GET /api/projects` (list), `GET /api/projects/[id]` (project + its items), `POST /api/projects/[id]/items` (upload image + optional phrase → embed, generate description via Claude if `ANTHROPIC_API_KEY` is set, store, and return the top 3 most similar past items across all projects by cosine similarity on the embedding — the "resurfacing" feature). Frontend: a home page to create/list projects, and a project page with the upload form, resurfaced-related-pieces panel, and an evolution timeline (items ordered by upload time with image + description + phrase).
- Verified end-to-end with `npm run dev` + curl against real images from the Emotion Spiral test set: project creation, image upload, embedding computation, and the related-items similarity search all work correctly (a duplicate upload came back at 100% similarity, an unrelated earlier sketch at 54%). Description generation wasn't exercised since `ANTHROPIC_API_KEY` is still empty in `.env`/`.env.example` — add a real key there to turn it on.
- Test data and uploaded files from this verification pass were deleted afterward (`web/data/`, `web/public/uploads/` — both gitignored, not meant to ship).
- To run it: `cd web && npm install && npm run dev`, then open the printed localhost URL.
- Confirmed `npm run lint` and `npm run build` both pass clean (fixed two `react-hooks/set-state-in-effect` lint errors from the fetch-on-mount pattern on the home and project pages with targeted eslint-disable comments — a deliberate, intentional pattern here, not a bug).
- Restyled the raw browser file input on the upload form into a labeled dropzone-style control (button + selected filename), since the plain unstyled input read as "there's no way to add an image."
- Switched the description LLM from Claude to Gemini per request: swapped `@anthropic-ai/sdk` for `@google/genai` in `lib/describe.ts`, env var is now `GEMINI_API_KEY` (updated `web/.env` and `web/.env.example`). Also fixed a real bug found while testing this: a failed/erroring description call was throwing and crashing the whole upload with a 500 — wrapped it in try/catch so a description failure now just falls back to no description instead of blocking capture, which matters given the plan's "near-zero friction ingestion" requirement.
- Had to correct the model name once live: `gemini-2.5-flash` is no longer available to new API keys; Google's error pointed to `gemini-3.6-flash`, which is now in use.
- Verified end-to-end with a real image (a moodboard grid of colorful lamp designs): Gemini produced an accurate one-line description ("a collection of colorful, modern, and uniquely shaped light fixtures..."), confirming the description feature genuinely works now, not just that it doesn't crash.
- Added two more requested features: a click-to-expand lightbox for timeline images (click a thumbnail → full-size overlay with its description/note, click outside to close), and a **documentation generator** — a dedicated `/projects/[id]/documentation` page with a "Generate documentation" button that sends every entry's description/note/date to Gemini and asks it to write a short markdown-formatted narrative of how the project evolved (intro, chronological turning points, closing reflection), rendered with `react-markdown`. The result is saved on the project (`documentation`, `documentation_generated_at` columns) so it persists and can be regenerated.
- Verified with 3 real entries from Emotion Spiral: the generated documentation correctly picked out a real arc across the images (moodboard → 3D-printed prototypes → circuit/perfboard soldering) rather than just restating each entry — a good sign the "evolution" framing from the plan is actually working, not just template filler.
